# SYNTAX TEST "source.xy" "File with simple but complete test syntax"

var x = "foo"
# <--- keyword.control.xy
#       ^^^^^ source.xy string.xy ! constant.integer.xy

# Comments can be tested too
# ^ comment.line.xy

if (true) {
  var y = 4
# <~~--- keyword.control.xy
# ^^^ keyword.control.xy
}

# Another normal comment

var s = "hi @{name}"
#       ^^^^^^^^^^^^ string.quoted.double.xy
#           ^^^^^^^ variable.interpolation.xy
#             ^^^^ string.xy variable.interpolation.xy meta.label.xy
