### Unit Testing Syntax

#### Multiple Disjoint Caret Assertions

You can now include multiple disjoint caret assertions on one line. For example:

`^^  ^^^ source.xy`

This allows for more flexible unit testing configurations and improves syntax highlighting when dealing with complex expressions.